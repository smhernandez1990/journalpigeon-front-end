import { useParams, Link } from 'react-router'
import * as postService from '../../services/postService'

const PostList = () => {
  
  return (
    <div>we will dynamically list requested posts here</div>
  )
}

export default PostList